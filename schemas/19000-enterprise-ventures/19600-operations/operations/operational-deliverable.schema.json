{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "Operational Deliverable",
  "description": "Business operations context wrapper for canonical deliverable records.",
  "type": "object",
  "properties": {
    "id": { "type": "string" },
    "uuid": { "type": "string", "format": "uuid" },
    "business_id": { "type": "string" },
    "olp_deliverable_id": { "type": "string", "description": "Reference to core OLP deliverable schema" },
    "department_id": { "type": "string" },
    "owner_id": { "type": "string" },
    "metadata": { "type": "object", "additionalProperties": true }
  },
  "required": ["id", "uuid", "business_id"],
  "_phase1_todo": ["Link to OLP deliverable for full detail", "Define business-specific deliverable extensions"]
}
