{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "control (Compliance Risk)",
  "description": "Business compliance-risk schema for control.",
  "type": "object",
  "properties": {
    "id": { "type": "string" },
    "uuid": { "type": "string", "format": "uuid" },
    "business_id": { "type": "string" },
    "name": { "type": "string" },
    "owner_id": { "type": "string" },
    "review_date": { "type": "string", "format": "date" },
    "status": { "type": "string" },
    "metadata": { "type": "object", "additionalProperties": true }
  },
  "required": ["id", "uuid", "business_id", "name"],
  "_phase1_todo": ["Define control framework", "Define control review workflow", "Define control audit trail"]
}
