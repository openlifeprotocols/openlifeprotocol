{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "workflow pack (Business Registry)",
  "description": "Business registry schema for workflow-pack — reusable templates and reference data.",
  "type": "object",
  "properties": {
    "id": { "type": "string" },
    "uuid": { "type": "string", "format": "uuid" },
    "name": { "type": "string" },
    "version": { "type": "string" },
    "description": { "type": "string" },
    "status": { "type": "string", "enum": ["active", "draft", "archived"] },
    "metadata": { "type": "object", "additionalProperties": true }
  },
  "required": ["id", "uuid", "name"],
  "_phase1_todo": ["Define workflow-pack publish workflow", "Define workflow-pack versioning", "Define workflow-pack deployment"]
}
