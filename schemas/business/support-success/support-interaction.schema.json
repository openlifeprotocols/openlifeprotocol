{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "support interaction (Support Success)",
  "description": "Business support and success schema for support-interaction.",
  "type": "object",
  "properties": {
    "id": { "type": "string" },
    "uuid": { "type": "string", "format": "uuid" },
    "business_id": { "type": "string" },
    "customer_id": { "type": "string" },
    "owner_id": { "type": "string" },
    "status": { "type": "string" },
    "metadata": { "type": "object", "additionalProperties": true }
  },
  "required": ["id", "uuid", "business_id"],
  "_phase1_todo": ["Define support-interaction workflow", "Define support-interaction SLA tracking"]
}
