{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "cost (Business Finance)",
  "description": "Business finance context for cost. References commerce/cost.schema.json and adds business_id, department, cost_centre, and reporting period linkage.",
  "type": "object",
  "properties": {
    "id": { "type": "string" },
    "uuid": { "type": "string", "format": "uuid" },
    "business_id": { "type": "string" },
    "commerce_cost_id": { "type": "string", "description": "Reference to commerce/cost.schema.json" },
    "department_id": { "type": "string" },
    "cost_centre": { "type": "string" },
    "reporting_period": { "type": "string" },
    "metadata": { "type": "object", "additionalProperties": true }
  },
  "required": ["id", "uuid", "business_id"],
  "_phase1_todo": ["Link to commerce/cost for full detail", "Define business-specific cost rules"]
}
